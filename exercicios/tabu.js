// Tabuada do 6

const multiplicador = 6;
// let i = 0;
// Chegará até o valor desejado, caso será 10

for(let i = 0; i <=10; i++){
    const calc = multiplicador * i;
    console.log(`${multiplicador} x ${i} = ${calc}`); 
} 

// tabuada do 7

const multiplicador2 = 7;

for(let i = 0; i <= 1000; i++){
    const calc2 = multiplicador2 * i;
    console.log(`${multiplicador2} x ${i} = ${calc2}`);
}
